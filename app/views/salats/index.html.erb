<div class="container">
  <div class="row pl-3 pr-3 pb-3">
    <% @salats.map do |salat| %>
      <div class="col col-md-3 offset-md-1 col-12 line-container mt-3 pt-3 pb-3">
        <h1 class="text-center"> <%= link_to salat.name, salat_path(id: salat.id)  %> </h1>
      </div>
    <% end %>
  </div>
</div>
