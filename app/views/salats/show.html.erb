<div class="container text-center">
  <p class="text-center m-3 werd-desc"><%= @salat[:desc] %></p>
  <div class="mt-3">
    <% @salat.lines.map.with_index do |line, index| %>
      <div class="col col-12 line-container green werd  text-center text-center">
        <%= simple_format line.body %>
      </div>
      <span class="line-index werd-index"><%= index + 1 %> / <%= @salat.lines.size %> </span>
    <% end %>
  </div>

</div>
